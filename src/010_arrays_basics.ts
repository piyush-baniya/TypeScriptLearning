const a11: number[] = [1, 2, 3]; // T[]
const a22: Array<number> = [1, 2, 3]; // Array<T>

const score = [10, 20, 30];
// score.push("40"); // not possible to push strings
const mix: (string | number)[] = [1, "2", 3, "4"];
